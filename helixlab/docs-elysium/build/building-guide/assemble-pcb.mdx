---
sidebar_position: 4
description: 组装内胆
---

import ThemedImage from '@theme/ThemedImage';
import useBaseUrl from '@docusaurus/useBaseUrl';

# 组装内胆 {#assemble-pcb}

<ThemedImage
  alt="Docusaurus themed image"
  sources={{
    light: useBaseUrl('/img/elysium/building-guide/5_light.svg'),
    dark: useBaseUrl('/img/elysium/building-guide/5_dark.svg'),
  }}
/>

<img src={'/img/numbers/n_1_alternative.svg'} style={{ verticalAlign: '-0.16em', width: '18px' }} /> &nbsp; （选装）安装轴上垫，左右各一。

<img src={'/img/numbers/n_2_alternative.svg'} style={{ verticalAlign: '-0.16em', width: '18px' }} /> &nbsp; （选装）安装夹心棉，左右各一。

<img src={'/img/numbers/n_3_alternative.svg'} style={{ verticalAlign: '-0.16em', width: '18px' }} /> &nbsp; 安装卫星轴。

<img src={'/img/numbers/n_4_alternative.svg'} style={{ verticalAlign: '-0.16em', width: '18px' }} /> &nbsp; 安装轴体。

<img src={'/img/numbers/n_5_alternative.svg'} style={{ verticalAlign: '-0.16em', width: '18px' }} /> &nbsp; 安装 16 颗硅胶粒。

:::info

夹心棉、轴上垫左右区分:

<div className="responsive-image-grid" style={{ 
  display: 'grid',
  gridTemplateAreas: `"left main right" "bottom bottom bottom"`,
  alignItems: 'center',
  gap: '4px 12px', // 纵向间隔 横向间隔
  margin: '20px 0',
  gridTemplateColumns: '1fr auto 1fr', // 关键修改点
  width: '100%', // 确保grid容器占满父级宽度
}}>
  {/* 左侧文字 */}
  <span style={{ 
    gridArea: 'left',
    textAlign: 'right',
    justifySelf: 'end',
    fontSize: 10,
    fontWeight: 'bold',
    color: '#fff',
    whiteSpace: 'nowrap',
    marginRight: '8px',
  }}>左</span>

  {/* 图片主体 */}
  <div className="responsive-image-container" style={{ gridArea: 'main', position: 'relative',  width: '600px', justifySelf: 'center'}}>
    <ThemedImage
    alt="Docusaurus themed image"
    sources={{
    light: useBaseUrl('/img/elysium/building-guide/5_info_1.svg'),
    dark: useBaseUrl('/img/elysium/building-guide/5_info_1_dark.svg'),
  }}
/>
  </div>

  {/* 右侧文字 */}
  <span style={{ 
    gridArea: 'right',
    textAlign: 'left',
    justifySelf: 'start',
    fontSize: 10,
    fontWeight: 'bold',
    color: '#fff',
    whiteSpace: 'nowrap',
    marginLeft: '8px',
  }}>右</span>

  {/* 底部文字 */}
  <div style={{ 
    gridArea: 'bottom',
    textAlign: 'center',
    fontSize: 10,
    fontWeight: 'bold',
    color: '#fff',
    marginTop: '4px',
  }}>
    <Icon icon="fa-solid fa-caret-up" /> 夹心棉
  </div>
</div>

<div className="responsive-grid-container" style={{ 
  display: 'grid',
  gridTemplateAreas: `"left main right" "bottom bottom bottom"`,
  alignItems: 'center',
  gap: '4px 12px', // 纵向间隔 横向间隔
  margin: '20px 0',
  gridTemplateColumns: '1fr auto 1fr', // 关键修改点
  width: '100%', // 确保grid容器占满父级宽度
}}>
  {/* 左侧文字 */}
  <span style={{ 
    gridArea: 'left',
    textAlign: 'right',
    justifySelf: 'end',
    fontSize: 10,
    fontWeight: 'bold',
    color: '#fff',
    whiteSpace: 'nowrap',
    marginRight: '8px',
  }}>左</span>

  {/* 图片主体 */}
  <div className="responsive-image-container" style={{ gridArea: 'main', position: 'relative',  width: '600px', justifySelf: 'center'}}>
    <ThemedImage
    alt="Docusaurus themed image"
    sources={{
    light: useBaseUrl('/img/elysium/building-guide/5_info_2.svg'),
    dark: useBaseUrl('/img/elysium/building-guide/5_info_2_dark.svg'),
  }}
/>
  </div>

  {/* 右侧文字 */}
  <span style={{ 
    gridArea: 'right',
    textAlign: 'left',
    justifySelf: 'start',
    fontSize: 10,
    fontWeight: 'bold',
    color: '#fff',
    whiteSpace: 'nowrap',
    marginLeft: '8px',
  }}>右</span>

  {/* 底部文字 */}
  <div style={{ 
    gridArea: 'bottom',
    textAlign: 'center',
    fontSize: 10,
    fontWeight: 'bold',
    color: '#fff',
    marginTop: '4px',
  }}>
    <Icon icon="fa-solid fa-caret-up" /> 轴上垫
  </div>
</div>


:::

---

<ThemedImage
  alt="Docusaurus themed image"
  sources={{
    light: useBaseUrl('/img/elysium/building-guide/6_light.svg'),
    dark: useBaseUrl('/img/elysium/building-guide/6_dark.svg'),
  }}
/>

<img src={'/img/numbers/n_6.svg'} style={{ verticalAlign: '-0.xem', width: '18px' }} /> &nbsp; 用软排线连接左右两片 PCB：拉开 4 个连接座的黑色卡扣，塞入软排线并确保推至最深处，然后推回黑色卡扣。安装时请根据下方的警告确认软排线方向。

<img src={'/img/numbers/n_7.svg'} style={{ verticalAlign: '-0.16em', width: '18px' }} /> &nbsp; 将子母板连接线的一端连接至主板。

:::caution

在连接软排线时，务必留意方向。

{/* 外层容器 */}
<div style={{ 
  display: 'flex',
  gap: '60px', // 图片间距
  flexWrap: 'wrap', // 允许在小屏幕换行
}}>
  {/* 第一个图片组件 */}
  <div style={{ 
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: 'fit-content' 
  }}>
    <img 
      src={'/img/elysium/building-guide/fpc-1-top.svg'} 
      style={{ width: '125px' }} 
    />
      <span style={{ 
    textAlign: 'center', // 文字居中
    width: '100%', // 继承图片宽度
    marginTop: '8px', // 与图片间距
    fontSize: '10px',
    fontWeight: 'bold',
    color:'#fff'
  }}>
      <Icon icon="fa-solid fa-caret-up" /> 金手指面
    </span>
  </div>

  {/* 第二个图片组件 */}
  <div style={{ 
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    width: 'fit-content' 
  }}>
    <img 
      src={'/img/elysium/building-guide/fpc-2-top.svg'} 
      style={{ width: '100px', transform: 'translateY(67px)',}} 
    />
      <span style={{ 
    textAlign: 'center', // 文字居中
    width: '100%', // 继承图片宽度
    marginTop: '8px', // 与图片间距
    fontSize: '10px',
    fontWeight: 'bold',
    transform: 'translateY(67px)',
    color:'#fff'
  }}>
      <Icon icon="fa-solid fa-caret-up" /> 金手指面
    </span>
  </div>
</div>


步骤 <img src={'/img/numbers/n_6.svg'} style={{ verticalAlign: '-0.2em', width: '18px' }} /> 中，软排线的一面是金手指，另一面是凸起的薄片，在安装软排线时，**确保金手指面对着 PCB，这样你能够看到的是有薄片的那面。**

此外，软排线无法承受重量，因此在拿起内胆时，需要同时拿起左右两边，以避免对软排线造成拉扯或损坏。

步骤 <img src={'/img/numbers/n_7.svg'} style={{ verticalAlign: '-0.2em', width: '18px' }} /> 中的连接线采用防呆设计，有效防止反插。安装时请勿使用蛮力，如遇阻力，请调整方向重新插入。

:::

:::info

如果键盘的右半部分的某一排按键或灯光失灵，而左半部分正常，则通常是由于软排线未安装到位或损坏导致的。

其中，未安装到位的情况包括：

* 正反面颠倒。

* 金手指未能与连接器充分接触。

:::